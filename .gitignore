# Image archives — too heavy for git; reproduce via download scripts
assets/
# Web-ready derivatives built by `pnpm shrink` from assets/
assets-web/
# Poly Haven PBR textures — too heavy for git; reproduce via `pnpm textures`
public/textures/

# Regeneratable Wikimedia API response cache
metadata/.cache/

# Dependencies
node_modules/
.pnpm-store/

# Claude Code local scratch
.claude/

# Build artifacts
dist/
build/
out/
.next/
*.tsbuildinfo
next-env.d.ts

# Generated gallery data (rebuilt via `pnpm build:data`)
/src/data/artworks.json
/src/data/artists.json
/src/data/movements.json
/src/data/connections.json
/src/data/summary.json

# react-email dev server scratch dir
.react-email/

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
.idea/

# Logs / env
*.log
.env
.env.local
.env.*.local

# Lockfile cruft (keep package-lock.json / pnpm-lock.yaml)
yarn.lock
# claude code
.claude/settings.local.json
.claude/worktrees/

# standard defaults (mirrors ~/.config/git/ignore)
.DS_Store
._*
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
.idea/
.cursor/
*.swp
*~
node_modules/
__pycache__/
*.pyc
.venv/
venv/
*.log

# env files (mirrors ~/.config/git/ignore — defense in depth)
.env
.env.local
.env.dev
.env.development
.env.production
.env.staging
.env.test
.envrc.local
.vercel/
