# Copy to .env.local and adjust for your setup.

# Base URL the browser uses to fetch artwork images. Defaults (in code) to
# the rclone HTTP server run by docker-compose on :9100. Override to point
# at R2, a public CDN, or any other HTTP file host.
NEXT_PUBLIC_ASSETS_BASE_URL=http://localhost:9100
